// logic for form goes here

/* 
- prevent default
- on click we need to:
    - check if email is valid
    - check if message is valid
*/

const button = document.getElementById('submit');
console.log(button);